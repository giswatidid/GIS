(function(){
'use strict';
const VERSION='1.54.0';
const $=id=>document.getElementById(id);
const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const clone=value=>value==null?value:JSON.parse(JSON.stringify(value));
const state={layerId:null,scope:'all',rules:{duplicateFeatures:false,polygonOverlaps:false,lineSelfIntersections:false,danglingEndpoints:false},snapshot:null,original:null,working:null,originalReport:null,report:null,safeChanges:[],acceptedRepairs:[],preview:null,filter:'all',running:false,progress:null,worker:null,reject:null,lastError:''};
function api(){return window.EditPolygonGIS}
function core(){return window.EditPolygonGeometryHealthCore}
function layers(){return (api()?.getEditableLayers?.()||[]).filter(layer=>!layer.tableOnly)}
function collection(features){return {type:'FeatureCollection',features:clone(features||[])};}
function panel(){let el=$('geometryHealthPanel');if(el)return el;el=document.createElement('aside');el.id='geometryHealthPanel';el.className='geometry-health-panel';el.setAttribute('aria-label','Geometry Health');el.innerHTML='<div id="geometryHealthContent"></div>';document.body.appendChild(el);el.addEventListener('click',handleClick);el.addEventListener('change',handleChange);return el;}
function currentCollection(){return state.working||state.original||collection([]);}
function sourceFeature(id){return currentCollection().features.find(feature=>String(feature.id)===String(id))||state.original?.features?.find(feature=>String(feature.id)===String(id))||null;}
function formatNumber(value,digits=1){const n=Number(value);return Number.isFinite(n)?n.toLocaleString(undefined,{maximumFractionDigits:digits}):'—';}
function formatPercent(value){const n=Number(value);if(!Number.isFinite(n))return '—';return `${n>0?'+':''}${n.toFixed(Math.abs(n)<.1?2:1)}%`;}
function statusLabel(status){return status==='safe'?'Safe to fix':status==='review'?'Needs review':status==='manual'?'Manual review':'Ready';}
function statusClass(status){return `geometry-health-${status||'ready'}`;}
function geometryFamily(report){const t=report?.stats?.types||{};const parts=[];if((t.Point||0)+(t.MultiPoint||0))parts.push(`${(t.Point||0)+(t.MultiPoint||0)} point`);if((t.LineString||0)+(t.MultiLineString||0))parts.push(`${(t.LineString||0)+(t.MultiLineString||0)} line`);if((t.Polygon||0)+(t.MultiPolygon||0))parts.push(`${(t.Polygon||0)+(t.MultiPolygon||0)} polygon`);return parts.join(' · ')||'No supported geometry';}
function rulesDescription(){const labels=[];if(state.rules.duplicateFeatures)labels.push('duplicate features');if(state.rules.polygonOverlaps)labels.push('polygon overlaps');if(state.rules.lineSelfIntersections)labels.push('line self-crossings');if(state.rules.danglingEndpoints)labels.push('dangling line endpoints');return labels.length?`Standard checks + ${labels.join(', ')}`:'Standard geometry checks';}
function scopeOptions(){const snapshot=state.snapshot,selection=api()?.getSelection?.()||{ids:[]},selected=api()?.getSelectedFeature?.()?.fileId===state.layerId?(selection.ids||[]).length:0;return `<option value="all" ${state.scope==='all'?'selected':''}>Entire layer · ${Number(snapshot?.featureCount||0).toLocaleString()}</option><option value="filtered" ${state.scope==='filtered'?'selected':''}>Filtered records</option><option value="visible" ${state.scope==='visible'?'selected':''}>Visible records</option><option value="selected" ${state.scope==='selected'?'selected':''} ${selected?'':'disabled'}>Selected records · ${selected.toLocaleString()}</option>`;}
function renderHeader(){const layer=layers().find(x=>x.id===state.layerId);return `<header class="geometry-health-header"><div><span class="geometry-health-kicker">Geometry Health · v${VERSION}</span><strong>Check &amp; fix geometry</strong><small>${esc(layer?.name||'Choose a layer')}</small></div><button type="button" data-gh-action="close" aria-label="Close Geometry Health">×</button></header>`;}
function renderSetup(){const ls=layers();return `<section class="geometry-health-section geometry-health-setup"><div class="geometry-health-section-title"><div><h3>1. Choose what to check</h3><p>Standard checks are designed to work without GIS topology knowledge.</p></div></div><label>Layer<select id="geometryHealthLayer">${ls.map(layer=>`<option value="${esc(layer.id)}" ${layer.id===state.layerId?'selected':''}>${esc(layer.name)} · ${Number(layer.count||layer.featureCount||0).toLocaleString()} features</option>`).join('')}</select></label><label>Records<select id="geometryHealthScope">${scopeOptions()}</select></label><details class="geometry-health-advanced"><summary>Advanced layer rules</summary><p>These rules describe how a particular dataset should behave. They are not universal geometry errors.</p><label><input type="checkbox" data-gh-rule="duplicateFeatures" ${state.rules.duplicateFeatures?'checked':''}> Flag exact duplicate features</label><label><input type="checkbox" data-gh-rule="polygonOverlaps" ${state.rules.polygonOverlaps?'checked':''}> Polygons must not overlap</label><label><input type="checkbox" data-gh-rule="lineSelfIntersections" ${state.rules.lineSelfIntersections?'checked':''}> Lines must not cross themselves</label><label><input type="checkbox" data-gh-rule="danglingEndpoints" ${state.rules.danglingEndpoints?'checked':''}> Lines must not have dangling endpoints</label></details><div class="geometry-health-actions"><button class="primary" data-gh-action="check" ${state.running?'disabled':''}>${state.report?'Check again':'Check geometry'}</button>${state.running?'<button data-gh-action="cancel">Cancel</button>':''}</div>${state.progress?renderProgress():''}${!state.report&&state.lastError?`<div class="geometry-health-error">${esc(state.lastError)}</div>`:''}</section>`;}
function renderProgress(){const p=state.progress||{},pct=p.total?Math.max(0,Math.min(100,Math.round((p.completed/p.total)*100))):5;return `<div class="geometry-health-progress"><div><i style="width:${pct}%"></i></div><span>${esc(p.message||'Working…')}</span><strong>${p.total?`${pct}%`:''}</strong></div>`;}
function metric(status,label,count,help){return `<button type="button" class="geometry-health-metric ${statusClass(status)} ${state.filter===status?'active':''}" data-gh-filter="${status}"><strong>${Number(count||0).toLocaleString()}</strong><span>${esc(label)}</span><small>${esc(help)}</small></button>`;}
function renderOverview(){const r=state.report,c=r?.counts||{};if(!r)return '';const changed=!!state.working,robust=r.engine?.robust;const robustText=!robust?'':robust.status==='ready'?`<span class="geometry-health-engine ok">GEOS verified ${Number(robust.checked||0).toLocaleString()} polygon${robust.checked===1?'':'s'}</span>`:robust.status==='partial'?`<span class="geometry-health-engine attention">GEOS checked ${Number(robust.checked||0).toLocaleString()} · ${Number(robust.errors||0)} could not be verified</span>`:robust.status==='unavailable'?'<span class="geometry-health-engine attention">Robust GEOS check unavailable — structural checks completed</span>':'';return `<section class="geometry-health-section"><div class="geometry-health-section-title"><div><h3>2. Geometry result</h3><p>${esc(rulesDescription())} · ${esc(geometryFamily(r))} · ${Number(r.stats?.vertices||0).toLocaleString()} vertices</p></div>${changed?'<span class="geometry-health-working-badge">Repair preview</span>':''}</div>${robustText}<div class="geometry-health-metrics">${metric('ready','Ready',c.ready,'No problems found')}${metric('safe','Safe to fix',c.safe,'Shape-preserving cleanup')}${metric('review','Needs review',c.review,'Repair can change geometry')}${metric('manual','Manual review',c.manual,'No automatic decision')}</div><div class="geometry-health-result-note ${c.review||c.manual?'attention':'ok'}">${c.issues?`${c.issues.toLocaleString()} issue${c.issues===1?'':'s'} across ${c.checked.toLocaleString()} checked feature${c.checked===1?'':'s'}. Feature counts above are exclusive: each feature appears under its most serious issue.`:`All ${c.checked.toLocaleString()} checked feature${c.checked===1?' is':'s are'} ready to use for the checks selected.`}</div>${(r.warnings||[]).map(w=>`<div class="geometry-health-warning">${esc(w)}</div>`).join('')}${state.lastError?`<div class="geometry-health-error">${esc(state.lastError)}</div>`:''}</section>`;}
function filteredFeatureResults(){const results=state.report?.featureResults||[];if(state.filter==='all')return results.filter(r=>r.issueCount);return results.filter(r=>r.status===state.filter);}
function issueButton(issue){const locate=issue.location||issue.featureId?'<button type="button" data-gh-action="locate" data-feature-id="'+esc(issue.featureId)+'" data-issue-id="'+esc(issue.id)+'">Locate</button>':'';const repair=issue.risk==='review'&&issue.repair?`<button type="button" class="primary" data-gh-action="preview-repair" data-feature-id="${esc(issue.featureId)}" data-issue-id="${esc(issue.id)}">Preview repair</button>`:'';return locate+repair;}
function renderIssue(issue){return `<article class="geometry-health-issue ${statusClass(issue.risk)}" data-issue="${esc(issue.id)}"><div class="geometry-health-issue-main"><span class="geometry-health-risk">${esc(statusLabel(issue.risk))}</span><h4>${esc(issue.title)}</h4><p>${esc(issue.detail||issue.summary)}</p>${issue.path?`<small>${esc(issue.path)}</small>`:''}</div><div class="geometry-health-issue-actions">${issueButton(issue)}</div><details><summary>Technical detail</summary><code>${esc(issue.code)}</code>${issue.technical?`<p>${esc(issue.technical)}</p>`:''}${issue.robustReason?`<p><strong>GEOS:</strong> ${esc(issue.robustReason)}</p>`:''}${issue.rule&&issue.rule!=='standard'?`<p>Layer rule: ${esc(issue.rule)}</p>`:''}</details></article>`;}
function renderIssues(){if(!state.report)return '';const results=filteredFeatureResults();const filterButtons=['all','safe','review','manual'].map(key=>`<button type="button" data-gh-filter="${key}" class="${state.filter===key?'active':''}">${key==='all'?'All issues':statusLabel(key)}</button>`).join('');return `<section class="geometry-health-section geometry-health-issues"><div class="geometry-health-section-title"><div><h3>3. Understand the problems</h3><p>Select an issue to locate the affected feature and problem area on the map.</p></div></div><div class="geometry-health-filterbar">${filterButtons}</div><div class="geometry-health-issue-list">${results.length?results.map(result=>`<section class="geometry-health-feature-group"><header><strong>${esc(result.featureName)}</strong><span>${esc(result.geometryType)} · ${result.issueCount} issue${result.issueCount===1?'':'s'} · ${esc(statusLabel(result.status))}</span></header>${result.issues.filter(issue=>state.filter==='all'||issue.risk===state.filter).map(renderIssue).join('')}</section>`).join(''):'<div class="geometry-health-empty">No issues in this category.</div>'}</div></section>`;}
function changeRows(){const before=state.originalReport?.counts,after=state.report?.counts;if(!before||!after)return '';const row=(label,a,b)=>`<tr><th>${label}</th><td>${Number(a||0).toLocaleString()}</td><td>${Number(b||0).toLocaleString()}</td><td>${Number((b||0)-(a||0))>0?'+':''}${Number((b||0)-(a||0)).toLocaleString()}</td></tr>`;return `<table class="geometry-health-compare-table"><thead><tr><th></th><th>Before</th><th>Current preview</th><th>Change</th></tr></thead><tbody>${row('Features checked',before.checked,after.checked)}${row('Ready',before.ready,after.ready)}${row('Safe to fix',before.safe,after.safe)}${row('Needs review',before.review,after.review)}${row('Manual review',before.manual,after.manual)}${row('Issues',before.issues,after.issues)}</tbody></table>`;}
function renderSafeActions(){if(!state.report)return '';const c=state.report.counts,hasWorking=!!state.working,safeFeatureCount=(state.report.featureResults||[]).filter(result=>(result.issues||[]).some(issue=>issue.risk==='safe')).length;return `<section class="geometry-health-section"><div class="geometry-health-section-title"><div><h3>4. Fix safely</h3><p>Safe cleanup only removes exact redundancy or normalises coordinate order. It does not close boundaries, delete features or choose between ambiguous shapes.</p></div></div><div class="geometry-health-actions"><button class="primary" data-gh-action="safe-fix" ${!safeFeatureCount||state.running?'disabled':''}>${safeFeatureCount?`Fix safe issues (${safeFeatureCount} feature${safeFeatureCount===1?'':'s'})`:'No safe fixes needed'}</button>${hasWorking?'<button data-gh-action="reset">Reset repairs</button>':''}</div>${state.safeChanges.length?`<div class="geometry-health-change-log"><strong>${state.safeChanges.length.toLocaleString()} feature${state.safeChanges.length===1?'':'s'} cleaned safely</strong><ul>${state.safeChanges.slice(0,8).map(item=>`<li>${esc(item.featureName)} — ${esc(item.changes.join('; '))}</li>`).join('')}${state.safeChanges.length>8?`<li>+ ${(state.safeChanges.length-8).toLocaleString()} more</li>`:''}</ul></div>`:''}${hasWorking?changeRows():''}</section>`;}
function renderPreview(){const p=state.preview;if(!p)return '';const areaRow=Number.isFinite(Number(p.areaChangePercent))?`<div><span>Relative area change</span><strong>${formatPercent(p.areaChangePercent)}</strong></div>`:'';const lengthRow=Number.isFinite(Number(p.lengthChangePercent))?`<div><span>Relative length change</span><strong>${formatPercent(p.lengthChangePercent)}</strong></div>`:'';const engine=p.engine?`<div class="geometry-health-engine ${p.engine.fallback?'attention':'ok'}"><strong>Repair engine:</strong> ${esc(p.engine.name||p.engine.library||'Geometry engine')}${p.engine.version?` · ${esc(p.engine.version)}`:''}${p.engine.fallback?' · fallback mode':''}</div>`:'';const warnings=(p.warnings||[]).map(w=>`<div class="geometry-health-warning">${esc(w)}</div>`).join('');return `<section class="geometry-health-section geometry-health-review-card ${p.consequence==='high'?'high-change':''}"><div class="geometry-health-section-title"><div><h3>Repair preview</h3><p>${esc(p.description)}</p></div><span class="geometry-health-consequence">${p.consequence==='high'?'Significant change':p.consequence==='medium'?'Review change':'Low change'}</span></div>${engine}<div class="geometry-health-preview-metrics"><div><span>Geometry</span><strong>${esc(p.before?.type||'—')} → ${esc(p.after?.type||'Removed')}</strong></div><div><span>Vertices</span><strong>${formatNumber(p.before?.vertices,0)} → ${formatNumber(p.after?.vertices,0)}</strong><small>${Number(p.after?.vertices||0)-Number(p.before?.vertices||0)>0?'+':''}${Number(p.after?.vertices||0)-Number(p.before?.vertices||0)}</small></div>${areaRow}${lengthRow}</div>${p.consequence==='high'?'<div class="geometry-health-warning"><strong>Review required.</strong> This proposal materially changes or removes geometry. Compare the purple dashed preview with the selected original feature before accepting it.</div>':'<div class="geometry-health-note">The purple dashed geometry on the map is the proposed repaired result. The original feature remains unchanged until you create a repaired layer.</div>'}${warnings}<div class="geometry-health-actions"><button class="primary" data-gh-action="accept-repair">Accept this repair</button><button data-gh-action="discard-preview">Discard preview</button></div>${p.remainingIssues?.length?`<details class="geometry-health-remaining"><summary>${p.remainingIssues.length} issue${p.remainingIssues.length===1?'':'s'} would remain on this feature</summary>${p.remainingIssues.slice(0,8).map(issue=>`<p><strong>${esc(issue.title)}</strong> — ${esc(issue.detail||issue.summary)}</p>`).join('')}</details>`:'<div class="geometry-health-ok-inline">This proposal passes the standard checks that can be evaluated on this feature.</div>'}</section>`;}
function renderOutput(){if(!state.report)return '';const changed=!!state.working||state.acceptedRepairs.length||state.safeChanges.length;return `<section class="geometry-health-section geometry-health-output"><div class="geometry-health-section-title"><div><h3>5. Create repaired layer</h3><p>The original layer is preserved. The result becomes a normal GIS layer with validation provenance and can be undone.</p>${state.scope!=='all'?`<p class="geometry-health-scope-note">Only the ${esc(state.scope)} records being checked will be included in this output layer.</p>`:''}</div></div><label>Output layer name<input id="geometryHealthOutputName" value="${esc((state.snapshot?.name||'Layer')+' — repaired')}"></label><div class="geometry-health-actions"><button class="primary" data-gh-action="create-layer" ${!changed||state.running?'disabled':''}>Create repaired layer</button><button data-gh-action="report-json">Export report JSON</button><button data-gh-action="report-csv">Export issue CSV</button></div>${!changed?'<p class="geometry-health-note">Run a safe fix or accept at least one reviewed repair before creating a repaired layer.</p>':`<p class="geometry-health-note">Current preview: ${state.report.counts.ready.toLocaleString()} ready · ${state.report.counts.safe.toLocaleString()} safe cleanup remaining · ${state.report.counts.review.toLocaleString()} review · ${state.report.counts.manual.toLocaleString()} manual.</p>${state.report.counts.review||state.report.counts.manual?'<div class="geometry-health-warning"><strong>Unresolved issues remain.</strong> They will stay in the repaired layer and are recorded in the report; nothing is hidden or silently discarded.</div>':''}`}</section>`;}
function render(){const el=panel(),content=$('geometryHealthContent');if(!content)return;el.classList.add('active');content.innerHTML=renderHeader()+renderSetup()+renderOverview()+renderPreview()+renderSafeActions()+renderIssues()+renderOutput();}
function refreshSnapshot(){if(!state.layerId)return;state.snapshot=api()?.getGeometryHealthLayer?.(state.layerId,{scope:state.scope});if(!state.snapshot)throw new Error('Could not read this layer.');state.original=collection(state.snapshot.features);state.working=null;state.report=null;state.originalReport=null;state.safeChanges=[];state.acceptedRepairs=[];state.preview=null;state.lastError='';}
function cancelWorker(){if(state.worker){try{state.worker.terminate();}catch(_){ }state.worker=null;}if(state.reject){state.reject(new Error('Geometry Health processing was cancelled.'));state.reject=null;}state.running=false;state.progress=null;render();}
function runWorker(action,payload={}){
  cancelWorkerSilently();state.running=true;state.lastError='';state.progress={completed:0,total:0,message:'Preparing…'};render();const id=Date.now()+Math.random();
  return new Promise((resolve,reject)=>{const worker=new Worker('assets/gis-geometry-health-worker.js?v=20260807-geometry-health-1540');state.worker=worker;state.reject=reject;worker.onmessage=event=>{const msg=event.data||{};if(msg.id!==id)return;if(msg.type==='progress'){state.progress={completed:msg.completed,total:msg.total,message:msg.message};render();return;}if(msg.type==='error'){finish();reject(new Error(msg.error||'Geometry Health worker failed.'));return;}if(msg.type==='result'){finish();resolve(msg.result);}};worker.onerror=event=>{finish();reject(new Error(event.message||'Geometry Health worker failed.'));};worker.postMessage({id,action,...payload,options:{rules:clone(state.rules),robustEngine:true}});function finish(){try{worker.terminate();}catch(_){ }if(state.worker===worker)state.worker=null;state.reject=null;state.running=false;state.progress=null;render();}});
}
function cancelWorkerSilently(){if(state.worker){try{state.worker.terminate();}catch(_){ }state.worker=null;}state.reject=null;state.running=false;state.progress=null;}
function wasCancelled(error){return error?.message==='Geometry Health processing was cancelled.';}
async function check(){try{state.preview=null;api()?.clearGeometryHealthOverlay?.();state.snapshot=api()?.getGeometryHealthLayer?.(state.layerId,{scope:state.scope});state.original=collection(state.snapshot.features);state.working=null;state.safeChanges=[];state.acceptedRepairs=[];const report=await runWorker('validate',{collection:state.original});state.report=report;state.originalReport=clone(report);state.filter='all';render();}catch(error){if(wasCancelled(error))return;state.lastError=error.message||String(error);render();}}
async function safeFix(){try{state.preview=null;api()?.clearGeometryHealthOverlay?.();const base=currentCollection();const result=await runWorker('repairSafe',{collection:base});state.working=result.collection;state.safeChanges.push(...(result.changeLog||[]));state.report=result.report;if(!state.originalReport)state.originalReport=clone(result.report);render();}catch(error){if(wasCancelled(error))return;state.lastError=error.message||String(error);render();}}
function issueById(id){return state.report?.issues?.find(issue=>issue.id===id)||null;}
async function previewRepair(issue){try{const feature=sourceFeature(issue.featureId);if(!feature)throw new Error('Feature not found in the current repair preview.');api()?.focusGeometryHealthIssue?.(state.layerId,issue.featureId,issue.location);const result=await runWorker('previewReview',{feature,issue});state.preview=result;if(result.feature)api()?.previewGeometryHealthProposal?.(result.feature);else api()?.clearGeometryHealthOverlay?.();render();}catch(error){if(wasCancelled(error))return;state.lastError=error.message||String(error);render();}}
async function acceptPreview(){const p=state.preview;if(!p)return;try{const base=clone(currentCollection());const index=base.features.findIndex(feature=>String(feature.id)===String(p.featureId));if(index<0)throw new Error('The feature changed before the repair could be accepted.');if(p.removed)base.features.splice(index,1);else base.features[index]=clone(p.feature);state.working=base;state.acceptedRepairs.push({featureId:p.featureId,issueId:p.issueId,action:p.action,description:p.description,consequence:p.consequence,areaChangePercent:p.areaChangePercent,lengthChangePercent:p.lengthChangePercent,engine:clone(p.engine||null),warnings:clone(p.warnings||[])});state.preview=null;api()?.clearGeometryHealthOverlay?.();state.report=await runWorker('validate',{collection:state.working});render();}catch(error){if(wasCancelled(error))return;state.lastError=error.message||String(error);render();}}
async function recheckCurrent(){try{state.preview=null;api()?.clearGeometryHealthOverlay?.();const report=await runWorker('validate',{collection:currentCollection()});state.report=report;if(!state.originalReport)state.originalReport=clone(report);render();}catch(error){if(wasCancelled(error))return;state.lastError=error.message||String(error);render();}}
function reset(){state.working=null;state.safeChanges=[];state.acceptedRepairs=[];state.preview=null;state.report=clone(state.originalReport);state.lastError='';api()?.clearGeometryHealthOverlay?.();render();}
function close(){cancelWorkerSilently();api()?.clearGeometryHealthOverlay?.();$('geometryHealthPanel')?.classList.remove('active');}
function createLayer(){try{const name=$('geometryHealthOutputName')?.value?.trim()||`${state.snapshot?.name||'Layer'} — repaired`;const issueSummary={};for(const issue of state.report?.issues||[]){const key=`${issue.risk}:${issue.code}`;issueSummary[key]=(issueSummary[key]||0)+1;}const reportMeta={beforeCounts:clone(state.originalReport?.counts||null),afterCounts:clone(state.report?.counts||null),beforeEngine:clone(state.originalReport?.engine||null),afterEngine:clone(state.report?.engine||null),beforeStats:clone(state.originalReport?.stats||null),afterStats:clone(state.report?.stats||null),warnings:clone(state.report?.warnings||[]),unresolvedIssueSummary:issueSummary};const result=api()?.createGeometryHealthLayer?.(state.layerId,currentCollection(),{name,scope:state.scope,report:reportMeta,changeLog:state.safeChanges,acceptedRepairs:state.acceptedRepairs,rules:state.rules});if(!result)throw new Error('Could not create the repaired layer.');close();}catch(error){state.lastError=error.message||String(error);render();}}
function download(name,text,type){const blob=new Blob([text],{type}),url=URL.createObjectURL(blob),a=document.createElement('a');a.href=url;a.download=name;document.body.appendChild(a);a.click();a.remove();setTimeout(()=>URL.revokeObjectURL(url),1000);}
function exportJson(){if(!state.report)return;const payload={kind:'editpolygon-geometry-health-report',version:VERSION,createdAt:new Date().toISOString(),layer:{id:state.layerId,name:state.snapshot?.name,scope:state.scope},rules:clone(state.rules),before:clone(state.originalReport),current:clone(state.report),safeChanges:clone(state.safeChanges),acceptedRepairs:clone(state.acceptedRepairs)};download(`${safeName(state.snapshot?.name||'layer')}_geometry_health.json`,JSON.stringify(payload,null,2),'application/json;charset=utf-8');}
function csvEscape(v){const s=String(v??'');return /[",\n]/.test(s)?`"${s.replace(/"/g,'""')}"`:s;}
function exportCsv(){if(!state.report)return;const rows=[['feature_id','feature_name','geometry_type','status','issue_code','issue_title','description','path','rule','automatic_repair']];for(const issue of state.report.issues||[])rows.push([issue.featureId,issue.featureName,issue.geometryType,issue.risk,issue.code,issue.title,issue.detail||issue.summary,issue.path,issue.rule,issue.repair?.action||'']);download(`${safeName(state.snapshot?.name||'layer')}_geometry_issues.csv`,rows.map(row=>row.map(csvEscape).join(',')).join('\n'),'text/csv;charset=utf-8');}
function safeName(value){return String(value||'layer').trim().replace(/[^A-Za-z0-9._-]+/g,'_').replace(/^_+|_+$/g,'')||'layer';}
function locate(issue){api()?.focusGeometryHealthIssue?.(state.layerId,issue.featureId,issue.location);}
function handleClick(event){const filter=event.target.closest('[data-gh-filter]');if(filter){state.filter=filter.dataset.ghFilter;render();return;}const btn=event.target.closest('[data-gh-action]');if(!btn)return;const action=btn.dataset.ghAction;if(action==='close')close();else if(action==='check')check();else if(action==='cancel')cancelWorker();else if(action==='safe-fix')safeFix();else if(action==='reset')reset();else if(action==='locate'){const issue=issueById(btn.dataset.issueId);if(issue)locate(issue);}else if(action==='preview-repair'){const issue=issueById(btn.dataset.issueId);if(issue)previewRepair(issue);}else if(action==='accept-repair')acceptPreview();else if(action==='discard-preview'){state.preview=null;api()?.clearGeometryHealthOverlay?.();render();}else if(action==='create-layer')createLayer();else if(action==='report-json')exportJson();else if(action==='report-csv')exportCsv();}
function handleChange(event){if(event.target.id==='geometryHealthLayer'){state.layerId=event.target.value;state.scope='all';refreshSnapshot();render();}else if(event.target.id==='geometryHealthScope'){state.scope=event.target.value;refreshSnapshot();render();}else if(event.target.matches('[data-gh-rule]')){state.rules[event.target.dataset.ghRule]=event.target.checked;if(state.report)recheckCurrent();}}
function open(layerId){const ls=layers();if(!ls.length){alert('Import or create a spatial layer before checking geometry.');return;}state.layerId=ls.some(layer=>layer.id===layerId)?layerId:(api()?.getSelectedFeature?.()?.fileId||ls[0].id);state.scope='all';state.filter='all';refreshSnapshot();render();}
function wireLegacyButton(){const button=$('validatorOpenBtn');if(!button)return;button.textContent='Check & fix geometry';button.title='Open Geometry Health for a project layer';button.addEventListener('click',event=>{event.preventDefault();event.stopImmediatePropagation();open(api()?.getSelectedFeature?.()?.fileId);},true);}
function keyboard(event){if(event.key==='Escape'&&$('geometryHealthPanel')?.classList.contains('active')){if(state.preview){state.preview=null;api()?.clearGeometryHealthOverlay?.();render();}else close();}}
window.addEventListener('keydown',keyboard,true);wireLegacyButton();
window.EditPolygonGeometryHealth=Object.freeze({version:VERSION,open,close,check,getState:()=>clone({layerId:state.layerId,scope:state.scope,rules:state.rules,report:state.report,originalReport:state.originalReport,safeChanges:state.safeChanges,acceptedRepairs:state.acceptedRepairs})});
})();
