(function(global){
'use strict';
const VERSION='1.56.1';
const GEOS_WASM_VERSION='3.1.1';
const clone=value=>value==null?value:JSON.parse(JSON.stringify(value));
function assertGeos(geos){
  const required=['GEOSGeoJSONReader_create','GEOSGeoJSONReader_readGeometry','GEOSGeoJSONReader_destroy','GEOSGeoJSONWriter_create','GEOSGeoJSONWriter_writeGeometry','GEOSGeoJSONWriter_destroy','GEOSGeom_destroy','GEOSFree','GEOSisValidReason','GEOSMakeValid'];
  if(!geos?.Module)throw new Error('GEOS-WASM did not expose its Emscripten module.');
  for(const name of required)if(typeof geos[name]!=='function')throw new Error(`GEOS-WASM is missing ${name}.`);
  if(typeof geos.Module._malloc!=='function'||typeof geos.Module._free!=='function'||typeof geos.Module.stringToUTF8!=='function'||typeof geos.Module.UTF8ToString!=='function')throw new Error('GEOS-WASM string memory helpers are unavailable.');
}
function utf8Size(text){return new TextEncoder().encode(String(text)).length+1;}
function allocString(geos,text){const value=String(text),size=utf8Size(value),ptr=geos.Module._malloc(size);if(!ptr)throw new Error('GEOS-WASM could not allocate input memory.');geos.Module.stringToUTF8(value,ptr,size);return ptr;}
function readGeometry(geos,geometry){assertGeos(geos);let reader=0,input=0,geom=0;try{reader=geos.GEOSGeoJSONReader_create();if(!reader)throw new Error('GEOS could not create a GeoJSON reader.');input=allocString(geos,JSON.stringify(geometry));geom=geos.GEOSGeoJSONReader_readGeometry(reader,input);if(!geom)throw new Error('GEOS could not read this GeoJSON geometry.');return geom;}finally{if(input)geos.Module._free(input);if(reader)geos.GEOSGeoJSONReader_destroy(reader);}}
function writeGeometry(geos,geom){if(!geom)throw new Error('GEOS returned an empty geometry pointer.');let writer=0,textPtr=0;try{writer=geos.GEOSGeoJSONWriter_create();if(!writer)throw new Error('GEOS could not create a GeoJSON writer.');textPtr=geos.GEOSGeoJSONWriter_writeGeometry(writer,geom,-1);if(!textPtr)throw new Error('GEOS could not write the result geometry.');return JSON.parse(geos.Module.UTF8ToString(textPtr));}finally{if(textPtr)geos.GEOSFree(textPtr);if(writer)geos.GEOSGeoJSONWriter_destroy(writer);}}
function geometryEmpty(geometry){if(!geometry)return true;if(geometry.type==='GeometryCollection')return !(geometry.geometries||[]).some(item=>!geometryEmpty(item));if(geometry.type==='Point')return !Array.isArray(geometry.coordinates)||geometry.coordinates.length<2;return !Array.isArray(geometry.coordinates)||geometry.coordinates.length===0;}
function validity(geos,geometry){let geom=0,reasonPtr=0;try{geom=readGeometry(geos,geometry);reasonPtr=geos.GEOSisValidReason(geom);if(!reasonPtr)throw new Error('GEOS could not evaluate geometry validity.');const reason=geos.Module.UTF8ToString(reasonPtr)||'Unknown GEOS validity result';return {valid:/^valid geometry$/i.test(reason.trim()),reason};}finally{if(reasonPtr)geos.GEOSFree(reasonPtr);if(geom)geos.GEOSGeom_destroy(geom);}}
function collectPolygonParts(geometry,parts,stats){if(!geometry)return;if(geometry.type==='Polygon'){parts.push(clone(geometry.coordinates));return;}if(geometry.type==='MultiPolygon'){for(const coordinates of geometry.coordinates||[])parts.push(clone(coordinates));return;}if(geometry.type==='GeometryCollection'){for(const child of geometry.geometries||[])collectPolygonParts(child,parts,stats);return;}if(geometry.type==='LineString'||geometry.type==='MultiLineString')stats.lineParts++;else if(geometry.type==='Point'||geometry.type==='MultiPoint')stats.pointParts++;else stats.otherParts++;}
function polygonalResult(raw){const parts=[],stats={lineParts:0,pointParts:0,otherParts:0};collectPolygonParts(raw,parts,stats);const geometry=parts.length===1?{type:'Polygon',coordinates:parts[0]}:parts.length>1?{type:'MultiPolygon',coordinates:parts}:null;return {geometry,discardedLowerDimensionalParts:stats.lineParts+stats.pointParts,discardedOtherParts:stats.otherParts,polygonPartCount:parts.length};}
function makeValidGeometry(geos,geometry){let inputGeom=0,repairedGeom=0;try{inputGeom=readGeometry(geos,geometry);repairedGeom=geos.GEOSMakeValid(inputGeom);if(!repairedGeom)throw new Error('GEOS MakeValid could not produce a result.');return writeGeometry(geos,repairedGeom);}finally{if(repairedGeom)geos.GEOSGeom_destroy(repairedGeom);if(inputGeom)geos.GEOSGeom_destroy(inputGeom);}}
function makeValid(geos,geometry){let inputGeom=0,repairedGeom=0,reasonBeforePtr=0,reasonAfterPtr=0;try{inputGeom=readGeometry(geos,geometry);reasonBeforePtr=geos.GEOSisValidReason(inputGeom);const reasonBefore=reasonBeforePtr?geos.Module.UTF8ToString(reasonBeforePtr):'Unknown';repairedGeom=geos.GEOSMakeValid(inputGeom);if(!repairedGeom)throw new Error('GEOS MakeValid could not produce a result.');const rawGeometry=writeGeometry(geos,repairedGeom);reasonAfterPtr=geos.GEOSisValidReason(repairedGeom);const reasonAfter=reasonAfterPtr?geos.Module.UTF8ToString(reasonAfterPtr):'Unknown',polygonal=polygonalResult(rawGeometry);return {...polygonal,rawGeometry,reasonBefore,reasonAfter,validAfter:/^valid geometry$/i.test(String(reasonAfter).trim()),engine:{name:'GEOS MakeValid',library:'geos-wasm',version:GEOS_WASM_VERSION}};}finally{if(reasonBeforePtr)geos.GEOSFree(reasonBeforePtr);if(reasonAfterPtr)geos.GEOSFree(reasonAfterPtr);if(repairedGeom)geos.GEOSGeom_destroy(repairedGeom);if(inputGeom)geos.GEOSGeom_destroy(inputGeom);}}
function binaryGeometry(geos,functionName,a,b,...args){const fn=geos?.[functionName];if(typeof fn!=='function')throw new Error(`GEOS-WASM does not expose ${functionName}.`);let ga=0,gb=0,result=0;try{ga=readGeometry(geos,a);gb=readGeometry(geos,b);result=fn(ga,gb,...args);if(!result)throw new Error(`${functionName} failed.`);return writeGeometry(geos,result);}finally{if(result)geos.GEOSGeom_destroy(result);if(gb)geos.GEOSGeom_destroy(gb);if(ga)geos.GEOSGeom_destroy(ga);}}
function unaryGeometry(geos,functionName,geometry,...args){const fn=geos?.[functionName];if(typeof fn!=='function')throw new Error(`GEOS-WASM does not expose ${functionName}.`);let input=0,result=0;try{input=readGeometry(geos,geometry);result=fn(input,...args);if(!result)throw new Error(`${functionName} failed.`);return writeGeometry(geos,result);}finally{if(result)geos.GEOSGeom_destroy(result);if(input)geos.GEOSGeom_destroy(input);}}
const intersection=(geos,a,b)=>binaryGeometry(geos,'GEOSIntersection',a,b);
const difference=(geos,a,b)=>binaryGeometry(geos,'GEOSDifference',a,b);
const symDifference=(geos,a,b)=>binaryGeometry(geos,'GEOSSymDifference',a,b);
const union=(geos,a,b)=>binaryGeometry(geos,'GEOSUnion',a,b);
function unaryUnion(geos,geometries){const list=(geometries||[]).filter(Boolean);if(!list.length)return null;if(list.length===1)return clone(list[0]);return unaryGeometry(geos,'GEOSUnaryUnion',{type:'GeometryCollection',geometries:list});}
const simplify=(geos,geometry,tolerance)=>unaryGeometry(geos,'GEOSTopologyPreserveSimplify',geometry,Number(tolerance));
const densify=(geos,geometry,tolerance)=>unaryGeometry(geos,'GEOSDensify',geometry,Number(tolerance));
const snap=(geos,geometry,target,tolerance)=>binaryGeometry(geos,'GEOSSnap',geometry,target,Number(tolerance));
function distance(geos,a,b){if(typeof geos?.GEOSDistance!=='function')throw new Error('GEOS-WASM does not expose GEOSDistance.');if(!geos.Module?.HEAPF64)throw new Error('GEOS-WASM does not expose floating-point memory for distance results.');let ga=0,gb=0,ptr=0;try{ga=readGeometry(geos,a);gb=readGeometry(geos,b);ptr=geos.Module._malloc(8);if(!ptr)throw new Error('GEOS-WASM could not allocate distance memory.');if(geos.GEOSDistance(ga,gb,ptr)!==1)throw new Error('GEOSDistance failed.');return Number(geos.Module.HEAPF64[ptr>>3]);}finally{if(ptr)geos.Module._free(ptr);if(gb)geos.GEOSGeom_destroy(gb);if(ga)geos.GEOSGeom_destroy(ga);}}
function locationFromReason(reason){const text=String(reason||''),match=text.match(/\[\s*(-?\d+(?:\.\d+)?(?:e[+-]?\d+)?)\s+(-?\d+(?:\.\d+)?(?:e[+-]?\d+)?)\s*\]/i);if(!match)return null;const x=Number(match[1]),y=Number(match[2]);return Number.isFinite(x)&&Number.isFinite(y)?[x,y]:null;}
const api=Object.freeze({VERSION,GEOS_WASM_VERSION,assertGeos,readGeometry,writeGeometry,geometryEmpty,validity,makeValid,makeValidGeometry,polygonalResult,intersection,difference,symDifference,union,unaryUnion,simplify,densify,snap,distance,locationFromReason});
global.EditPolygonGeosAdapter=api;if(typeof module!=='undefined'&&module.exports)module.exports=api;
})(typeof self!=='undefined'?self:globalThis);
