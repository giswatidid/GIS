# v1.55.1.1 changed files

OpenLayers parity live-startup hotfix. No files are deleted.

## Runtime / deployment

- `docs/assets/editpolygon-map-adapter.js` — removes the browser-global `ol.control.defaults()` / `ol.interaction.defaults()` assumption; OpenLayers now starts with EditPolygon-owned controls and native default interactions.
- `docs/index.html` — bumps the local asset cache key so GitHub Pages clients receive the fixed map adapter.
- `package.json` — hotfix version metadata.

## Regression coverage

- `tests/browser-openlayers-parity-smoke.py` — now mirrors the CDN global namespace without `control.defaults` / `interaction.defaults`.
- `tests/map-adapter.test.mjs` — hotfix runtime version expectation.
- `tests/release-cache.test.mjs`
- `tests/typed-fields-integration.test.mjs`
- `tests/gis-crs-integration.test.mjs`
- `tests/gis-remote-source-integration.test.mjs`
- `tests/render-performance.test.mjs` — updated release-cache expectations.

## Documentation

- `README.md` — current baseline updated.
- `GIS_OPENLAYERS_PARITY_HOTFIX_NOTES.md` — live failure, cause and fix.

## Validation

- Repository integration checks pass.
- 155/155 JavaScript tests pass.
- Leaflet map-adapter browser smoke passes.
- OpenLayers parity browser smoke passes.
- CRS, remote-source, typed-data, join and Geometry Health browser smoke suites pass.
